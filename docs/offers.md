# Offers

status 0 pending, 1 accepted, 2 rejected.

Offers can only be created against an active listing. `accept_offer` persists both the offer and listing. `reject_offer` leaves the listing active so another bid can land.
